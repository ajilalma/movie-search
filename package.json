{
  "name": "movie-search",
  "version": "1.0.0",
  "description": "A RAG engine to talk about movies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "initdb": "tsc && node dist/data/mongodb/insertdata.js --drop",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "got": "^15.0.0",
    "mongodb": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "author": "Ajilal M A",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  }
}
